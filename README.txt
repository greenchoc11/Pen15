The executable file should run the code, but needs to be in the folder
as per the zip file.

If you have any issues with the .exe as a backup, run the main.py file.

If there is an error , common fix is to make sure the .csv is completely empty on
first run of program on your machine.